{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@now/node"
        },
        {
            "src": "src/**/*",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "server.js" }
    ]
}